{
  "name": "song-recommender-memory",
  "version": "1.0.0",
  "description": "Song recommender with LangGraph memory persistence using LibSQL",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f postgres",
    "chat:igorRomero": "node --experimental-strip-types --env-file .env src/index.ts --user igorRomero",
    "chat:ana": "node --experimental-strip-types --env-file .env src/index.ts --user ana",
    "test": "node --experimental-strip-types --env-file .env --test tests/**/*.test.ts",
    "test:watch": "node --experimental-strip-types --env-file .env --test --watch tests/**/*.test.ts",
    "langgraph:serve": "npx @langchain/langgraph-cli dev"
  },
  "keywords": [
    "langchain",
    "langgraph",
    "memory",
    "libsql",
    "sqlite",
    "song-recommender",
    "openrouter"
  ],
  "author": "igorRomero",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.10.13",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=24.10.0"
  },
  "dependencies": {
    "@langchain/langgraph-checkpoint-postgres": "^1.0.0",
    "@libsql/client": "^0.17.0",
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^11.10.0",
    "knex": "^3.1.0",
    "@langchain/openai": "^1.2.5",
    "@openrouter/sdk": "^0.5.1",
    "@langchain/core": "^1.2.8",
    "@langchain/langgraph": "^1.4.10",
    "@types/node": "^24.10.13",
    "langchain": "^1.5.9"
  }
}
